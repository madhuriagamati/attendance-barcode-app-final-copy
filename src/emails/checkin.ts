import { fmtDate } from '@/lib/utils';
export function checkinHtml(opts:{studentName:string;schoolName:string;time:Date;type?:'checkin'|'checkout';}){const{studentName,schoolName,time,type='checkin'}=opts;const ts=fmtDate(time);return `<div style="font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial; color: #0f172a;"><h2>${schoolName}: ${type==='checkin'?'Check-In':'Check-Out'} Notice</h2><p>Hi there,</p><p>This message is to let you know that <strong>${studentName}</strong> has ${type==='checkin'?'checked in to':'checked out of'} class.</p><p><strong>Time:</strong> ${ts}</p><p>If you have any questions, reply to this email.</p><p style="margin-top: 16px;">— ${schoolName} Attendance</p></div>`;}
